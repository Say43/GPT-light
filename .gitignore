# Model weights: too large for git, archived as private Kaggle datasets instead
# (see checkpoints/README.md for how to obtain them)
*.pt

__pycache__/
*.pyc

# Claude Code harness runtime state, not project content
.claude/
